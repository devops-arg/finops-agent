# Development dependencies — install with: pip install -r requirements-dev.txt
# Runtime deps live in requirements.txt (kept lean for the production image).

# Tests
pytest==8.3.4
pytest-asyncio==0.25.0

# Lint + format
ruff==0.15.12

# Static type check
mypy==1.14.0
boto3-stubs==1.43.6
types-requests==2.33.0.20260508

# Pre-commit hooks
pre-commit==4.6.0
